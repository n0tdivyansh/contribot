{
  "name": "ai-github-contributor-agent",
  "version": "0.1.0",
  "description": "Safe-only MVP CLI for deterministic GitHub contributions.",
  "type": "module",
  "main": "./dist/cli.js",
  "bin": {
    "ai-contributor": "./dist/cli.js"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE",
    "CHANGELOG.md",
    "aica.config.example.json"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "clean": "node -e \"import('node:fs').then(fs => fs.rmSync('dist', { recursive: true, force: true }))\"",
    "build": "npm run clean && tsc -p tsconfig.build.json",
    "dev": "node --loader ts-node/esm src/cli.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "check": "npm run build && npm test",
    "pack:dry": "npm pack --dry-run",
    "release:check": "npm run check && npm run pack:dry",
    "prepublishOnly": "npm run release:check"
  },
  "keywords": [
    "github",
    "cli",
    "automation",
    "oss",
    "pull-request",
    "lint",
    "formatter"
  ],
  "license": "MIT",
  "dependencies": {
    "commander": "^14.0.3"
  },
  "devDependencies": {
    "@types/node": "^24.5.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.2",
    "vitest": "^4.1.3"
  }
}
