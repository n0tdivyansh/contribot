# Changelog

## Unreleased

- Added real-time React dashboard with full dark glassmorphism design system
- Added Express API backend (`server/server.ts`) to serve structured JSON logs to the dashboard
- Implemented real-time polling hook to monitor aggregated statistics, runs history, and run activity feed

## 0.1.0

- Initial public alpha release
- Standardized the public project identity around `Contribot`
- Added safe CLI commands: `init`, `doctor`, `scan`, `fix`, `run`, and `auto`
- Added deterministic scanning, fix orchestration, validation, and optional PR publishing
- Added structured JSON logging
- Added release documentation, CI, and contributor templates
